# Methods: are simply functions that are built into objects
mylist = [1,2,3]
mylist.append(4)
print(mylist)
mylist.pop()
print(mylist)
# You can see a list of all the Methods that an object has by using the . after
#calling the object
help(mylist.insert()) #You can use the built in help function to find out
# more about built in functions and methods assgined to an object
#Find out if there is a feature like this in java

# the python documentation website: https://docs.python.org/3.7/
# keeo the library reference of the documentationunder your pillow: study it
# frequently
